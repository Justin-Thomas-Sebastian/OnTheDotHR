function openNav() {
    document.getElementById("header-side-panel").style.width = "250px";
}

/* Set the width of the sidebar to 0 (hide it) */
function closeNav() {
    document.getElementById("header-side-panel").style.width = "0";
}